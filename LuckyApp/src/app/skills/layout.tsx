import type { Metadata } from "next";

export const metadata: Metadata = { title: "Skill Marketplace — Swarm" };

export default function SkillsLayout({ children }: { children: React.ReactNode }) {
    return children;
}
