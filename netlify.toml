[build]
  base = "SwarmApp"
  # Build handled by GitHub Actions — Netlify OOMs on 573 routes.
  # SwarmApp/netlify.toml has ignore = "exit 0" to skip auto-builds.
  command = "echo 'Skipped — deployed from GitHub Actions'"
  publish = "SwarmApp/.next"
